#ifndef STRINGUTIL_H
#define STRINGUTIL_H

#include <string.h>
#include <stdlib.h>

/* Returns the index of a character in
 * a string. Returns -1 if not found.
 */
int indexof(char*, int);

#endif /* STRINGUTIL_H */
